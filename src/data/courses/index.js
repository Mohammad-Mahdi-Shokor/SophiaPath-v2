// Dynamically populated from database backend export/all endpoint.
export const coursesData = [];

export const getCourseByDomain = (domain) => {
  return null;
};

export const getCourseById = (id) => {
  return null;
};
